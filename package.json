{
  "name": "tauri-signature-to-k8s-secret",
  "version": "1.0.0",
  "description": "GitHub Action to extract Tauri build signatures and store them in Kubernetes secrets",
  "main": "src/main.js",
  "scripts": {
    "build": "ncc build src/main.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "lint": "eslint src/**/*.js",
    "package": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourusername/tauri-signature-to-k8s-secret.git"
  },
  "keywords": [
    "github-action",
    "tauri",
    "kubernetes",
    "signatures",
    "deployment",
    "desktop-app"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}